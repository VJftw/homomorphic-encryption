load("@rules_go//go:def.bzl", "go_binary", "go_library")

go_library(
    name = "backend_lib",
    srcs = [
        "main.go",
        "router.go",
    ],
    importpath = "github.com/vjftw/homomorphic-encryption/backend",
    visibility = ["//visibility:private"],
    deps = [
        "//backend/controllers",
        "@com_github_facebookgo_inject//:go_default_library",
        "@com_github_gorilla_mux//:go_default_library",
        "@com_github_justinas_alice//:go_default_library",
        "@com_github_rs_cors//:go_default_library",
        "@com_github_sebest_xff//:go_default_library",
    ],
)

go_binary(
    name = "backend",
    embed = [":backend_lib"],
    visibility = ["//visibility:public"],
)
